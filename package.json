{
  "name": "hiragana-word-api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "check": "biome check --write ."
  },
  "dependencies": {
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "wrangler": "^4.38.0"
  }
}
